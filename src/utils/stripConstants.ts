// ── stripConstants.ts ──────────────────────────────────────────────────────────
// 碎纸机动画常量：将卡片纵向切割为若干条，每条独立执行消散动画。
// ────────────────────────────────────────────────────────────────────────────────

/** 碎裂效果卡片条数（将卡片纵向切割为 8 条） */
export const STRIP_COUNT = 8;

/** 碎裂动画总时长（毫秒） */
export const DURATION = 700;
